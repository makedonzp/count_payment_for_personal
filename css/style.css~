body {
  font-family: 'Arial', sans-serif;
  background-color: #f5f5f5;
  margin: 0;
  padding: 20px;
  color: #333;
}

.container {
  max-width: 1000px;
  margin: 0 auto;
  background-color: white;
  padding: 30px;
  border-radius: 10px;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
}

h1 {
  color: #e30611;
  text-align: center;
  margin-bottom: 30px;
}

h2 {
  color: #e30611;
  font-size: 18px;
  margin-bottom: 15px;
}

.performance-section, .experience-section, .days-section {
  margin-bottom: 25px;
  padding: 15px;
  background-color: #f9f9f9;
  border-radius: 8px;
}

.performance-options, .experience-options {
  display: flex;
  gap: 20px;
  margin-top: 10px;
}

.performance-option, .experience-option {
  display: flex;
  align-items: center;
  gap: 5px;
  cursor: pointer;
}

.categories-section {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 15px;
  margin-bottom: 25px;
}

.category {
  display: flex;
  flex-direction: column;
  min-height: 150px;
}

.category label {
  margin-bottom: 5px;
  font-weight: bold;
  color: #555;
}

.category input {
  padding: 8px 12px;
  border: 1px solid #ddd;
  border-radius: 4px;
  font-size: 16px;
}

select {
  padding: 8px 12px;
  border: 1px solid #ddd;
  border-radius: 4px;
  font-size: 16px;
  width: 100%;
  max-width: 300px;
}

#calculate-btn {
  background-color: #e30611;
  color: white;
  border: none;
  padding: 12px 25px;
  font-size: 16px;
  border-radius: 4px;
  cursor: pointer;
  margin-top: 20px;
  transition: background-color 0.3s;
}

#calculate-btn:hover {
  background-color: #c0050e;
}

.result-box {
  margin-top: 30px;
  padding: 20px;
  background-color: #f9f9f9;
  border-radius: 8px;
  text-align: center;
}

#salary-result {
  font-size: 24px;
  font-weight: bold;
  color: #e30611;
  margin-top: 10px;
}

@media (max-width: 768px) {
  .categories-section {
    grid-template-columns: 1fr;
  }

  .performance-options, .experience-options {
    flex-direction: column;
    gap: 10px;
  }
}
/* Добавляем стили для чекбоксов внутри категорий */
.category .performance-options {
  display: flex;
  flex-direction: column;
  gap: 5px;
  margin-top: 8px;
  font-size: 14px;
}

.category .performance-options label {
  display: flex;
  align-items: center;
  gap: 5px;
}

.days-info {
  margin-bottom: 10px;
  color: #666;
  font-size: 14px;
}

.days-info p {
  margin: 5px 0;
}
